snippet     fori
options     head
abbr        for (int x;...; x++) {}
    for (int ${1:i} = 0; $1 < ${2}; $1++) ${0:#:TARGET}

snippet     forri
options     head
abbr        for (int x; ...; x--) {}
    for (int ${1:i} = ${2}; $1 >= 0; $1--) ${0:#:TARGET}

snippet     sortv
options     head
abbr        for (v.begin(), v.end())
    sort(${1:v}.begin(), $1.end());
    ${0:#:TARGET}
